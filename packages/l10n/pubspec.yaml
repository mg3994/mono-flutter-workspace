name: l10n
description: Monorepo localization resources package.
version: 0.1.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: '^3.11.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  generate: true
